#include "Graphs.h"
#include "SupportFunctions.h"
#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main()
{
    Menu();
    //srand(time(0));
    //map<int, int> count;
    //for (int i = 0; i < 10000; i++) {
    //    int r = rand();
    //    count[PascalsDistribution(4, 0.8, r)]++;
    //}
    //for (const auto & el : count) {
    //    cout << el.first << " " << el.second << endl;
    //}
    return 0;
}